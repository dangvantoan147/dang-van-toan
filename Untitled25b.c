#include<stdio.h>

int main() {
	double a, b;
	// Use %lf format specifier to input a double variable
	printf ("/n Nhap 2 so :");
	scanf("%lf%lf", &a, &b);
	printf("a * b = %f", a * b);
	return 0;
}
